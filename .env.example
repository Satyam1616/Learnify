# Firebase Configuration
# Option 1: Use Firebase Service Account JSON as environment variable (Recommended for production)
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project",...}

# Option 2: Use Firebase Service Account file (For development)
# Place firebase-service-account.json in the server directory

# Use Firebase instead of memory storage (set to 'true' to enable Firebase in development)
USE_FIREBASE=false

# Production settings
NODE_ENV=development
PORT=5000